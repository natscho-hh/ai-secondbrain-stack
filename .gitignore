# Obsidian per-session state (never commit — it's window/pane layout, not settings)
**/.obsidian/workspace.json
**/.obsidian/workspace-mobile.json
.DS_Store
Thumbs.db
*.log

# Planning-tool scratch artifacts (briefs, reports, diffs — not part of the shipped template)
.superpowers/

# Diagram Design profile marker — points at a style profile that lives in the
# author's home directory, not in this repo. Useless to anyone who clones it.
.diagram-design
